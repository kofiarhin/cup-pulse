import { useState } from "react";import { useNavigate } from "react-router-dom";import { getAdminToken,setAdminToken } from "../api/adminApi";
export function AdminLoginPage(){const [token,setToken]=useState(getAdminToken());const navigate=useNavigate();if(getAdminToken())return <AdminDashboardPage/>;return <div className="mx-auto grid min-h-[70dvh] max-w-lg place-items-center px-4"><form className="w-full border-y border-white/10 py-10" onSubmit={e=>{e.preventDefault();if(token.trim()){setAdminToken(token);navigate("/admin/health")}}}><p className="text-xs font-bold uppercase tracking-[.2em] text-lime-300">Protected area</p><h1 className="mt-3 text-4xl font-black tracking-tight">Admin access</h1><label htmlFor="admin-token" className="mt-8 block text-sm font-bold">API token</label><input id="admin-token" type="password" value={token} onChange={e=>setToken(e.target.value)} className="mt-2 w-full rounded-xl border border-white/15 bg-white/5 px-4 py-3 outline-none focus:border-lime-300" autoComplete="current-password"/><p className="mt-2 text-sm text-stone-500">Stored for this browser session only.</p><button className="mt-6 w-full rounded-xl bg-lime-300 px-5 py-3 font-black text-stone-950 active:scale-[.98]">Continue</button></form></div>}
export function AdminDashboardPage(){return <section><p className="text-xs font-bold uppercase tracking-[.2em] text-lime-300">Operations</p><h2 className="mt-3 text-4xl font-black tracking-tight">Tournament control room</h2><p className="mt-4 max-w-2xl leading-7 text-stone-400">Monitor provider synchronization, API health, public announcements and featured tournament content.</p></section>}
